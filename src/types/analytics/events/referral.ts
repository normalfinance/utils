/**
 * Referral specific events.
 */
export enum ReferralEventName {
  // UI
  SubmitCreateReferralCode = 'Submit Create Referral Code',

  // API
  ReferralCreated = 'Referral Created',
  ReferralCodeCreated = 'Referral Code Created',
}
